#ifndef OPERATOR_HPP
# define OPERATOR_HPP

#include "IClient.hpp"
#include <iostream>

class Operator : public IClient {
private:
    /* data */
public:
    Operator(/* args */);
    ~Operator();
};

#endif